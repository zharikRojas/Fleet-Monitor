# PostgreSQL (desarrollo local — debe coincidir con docker-compose.yml)
DB_HOST_DEV=localhost
DB_PORT_DEV=5433
DB_NAME_DEV=fleetmonitor
DB_USER_DEV=fleet
DB_PASSWORD_DEV=fleet123

ASPNETCORE_ENVIRONMENT=Development

# JWT (tarde Día 1)
JWT_SECRET_KEY=FleetMonitor_Dev_Secret_Key_Min32Chars!
JWT_ISSUER=fleet-monitor
JWT_AUDIENCE=fleet-monitor-api
JWT_EXPIRATION_MINUTES=60

# Frontend (futuro)
VITE_API_BASE_URL=http://localhost:5080/api/
